<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Networking for WebRTC + signaling. ACCESS_NETWORK_STATE is required by
         WebRTC's NetworkMonitor — without it getActiveNetworkInfo() throws a
         SecurityException that crashes the native network thread (SIGABRT). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <!-- Screen capture foreground service. FOREGROUND_SERVICE_MEDIA_PROJECTION is
         required on Android 14 (API 34+); without the typed FGS, MediaProjection
         throws a SecurityException. POST_NOTIFICATIONS lets the FGS notification
         show on Android 13+ (the service still runs if it's denied). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Lets the capture service carry the camera FGS type so the host's camera
         can be opened on-demand while the app is backgrounded. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA"/>
    <!-- Same for the microphone, so the viewer can listen in on demand. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>

    <!-- Generic keep-alive foreground service for the viewer role (connectedDevice
         type is satisfied by the CHANGE_NETWORK_STATE permission above). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE"/>
    <!-- Lets the app ask to be exempted from battery optimization so a connected
         session survives the app being swiped off recents on aggressive OEMs. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <!-- Lets BootReceiver fire after the device restarts so anytime access can
         be automatically restored with a single tap on the notification. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- Optional front-camera "presence" feature: the host explicitly taps to
         share their camera so the viewer can see them (like a video call).
         Android shows the camera indicator while it's active. -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- Folder/file access for the host's shared-files feature. On Android ≤ 12
         (SDK ≤ 32) READ_EXTERNAL_STORAGE covers external storage; on Android 13+
         MANAGE_EXTERNAL_STORAGE is requested at runtime via system settings so
         the host can share arbitrary directories with the viewer. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage"
        xmlns:tools="http://schemas.android.com/tools"/>

    <application
        android:label="Sim Tool-Kit"
        android:name=".MaskApplication"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Foreground service that holds the MediaProjection while sharing.
             stopWithTask="false" keeps it (and the process + cached engine)
             running when the user swipes the app off recents, so the host session
             survives until it's explicitly closed. -->
        <service
            android:name=".ScreenCaptureService"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="mediaProjection|camera|microphone|connectedDevice" />

        <!-- Generic keep-alive foreground service for the viewer role; also
             survives task removal so the control link isn't dropped. -->
        <service
            android:name=".ConnectionService"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- Fires after BOOT_COMPLETED to restore anytime access when the device
             was rebooted while the host was armed. exported=false: Android still
             delivers this protected system broadcast to non-exported receivers. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
